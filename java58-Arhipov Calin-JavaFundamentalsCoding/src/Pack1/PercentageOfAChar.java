//Read a text from keyboard and print the percentage of space characters
// appearing in the text. (eg. for text 'Hello,
// !' the percentage printed should be 30)
package Pack1;

import java.util.Scanner;

public class PercentageOfAChar {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        String text = scanner.nextLine(); //salut ce faci

        int spaces = 0;

        //numaram cate spatii exista in text
        for (int i = 0; i < text.length(); i++) {
            if (text.charAt(i) == ' ') {
            }
            //270 studenti ; 84 sunt fete
        }
    }
}
